def somolmposto(taxalmposto,custo):
    inposto = taxalmposto / 100
    venda = custo * inposto
    return venda



taxa = (float(input('Digite a taxa:')))
result = somolmposto(taxa)
preso = custo(float(input('Digite a taxa:')))

print('O preso do produdo de venda é',venda)

#result = par(valor)